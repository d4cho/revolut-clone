import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const CountryPickView = () => {
    return (
        <View>
            <Text>CountryPickView</Text>
        </View>
    );
};

export default CountryPickView;

const styles = StyleSheet.create({});
